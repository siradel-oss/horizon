load("@rules_cc//cc:cc_binary.bzl", "cc_binary")

cc_binary(
    name = "xml_desc_generator",
    srcs = [
        "main.cpp",
    ],
    copts = [
        "-I$(GENDIR)/hrz/xml_desc_generator",
    ],
    linkopts = select({
        "@platforms//os:linux": ["-pthread"],
        "//conditions:default": [],
    }),
    visibility = ["//visibility:public"],
    deps = [
        "@protobuf",
        "@protobuf//src/google/protobuf/compiler:plugin",
    ],
)
