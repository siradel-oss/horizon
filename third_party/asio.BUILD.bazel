package(default_visibility = ["//visibility:public"])

cc_library(
    name = "asio",
    hdrs = glob([
        "asio/include/**/*.hpp",
        "asio/include/**/*.ipp",
    ]),
    includes = ["asio/include"],
    defines = ["ASIO_STANDALONE"],
    linkopts = select({
        "@platforms//os:windows": [],
        "//conditions:default": ["-pthread"],
    }),
)

exports_files(["asio/LICENSE_1_0.txt"])
