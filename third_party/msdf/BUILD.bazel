# The files are not retrieved from the GitHub repository
# because they have been tweaked.

cc_library(
    name = "msdf",
    srcs = ["src/msdf.c"],
    hdrs = ["include/msdf.h"],
    includes = ["include"],
    deps = [
        "//third_party/stb_truetype",
    ],
    visibility = ["//visibility:public"],
)

exports_files(["LICENSE"])
