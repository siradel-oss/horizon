# Added

* DTM layers can now be deactivated for a scene view by setting the property `disable_dtm` of [TerrainSettings]($proto) to `true`. This deactivates all DTM layers for the scene view, i.e. it forces the ground to be at elevation 0.
