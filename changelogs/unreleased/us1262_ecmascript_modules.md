# Changed

* Web: Modules are now ES Modules, instead of CommonJS modules.

# Upgrade notes

* Due to the switch to ES Modules, symbols from `@siradel/horizon-*` packages must be imported using the `import` syntax.
